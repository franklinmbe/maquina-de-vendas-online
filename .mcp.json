{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    },
    "postiz": {
      "type": "http",
      "url": "https://mcp.postiz.com/mcp",
      "headers": {
        "Authorization": "Bearer ${POSTIZ_API_KEY}"
      }
    }
  }
}
